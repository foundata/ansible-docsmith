# Please note: This file is just a dummy for testing. It might make no sense
# at all.

---

install_path: "/opt/myapp"
install_version: "stable"  # specs say "latest" - should trigger mismatch warning
install_force: true  # specs say false - should trigger mismatch warning
